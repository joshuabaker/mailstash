{
  "name": "mailstash",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @mailstash/web dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
