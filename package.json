{
  "name": "mailstash",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n worker,vite -c blue,green \"pnpm --filter @mailstash/web dev:worker\" \"pnpm --filter @mailstash/web dev\"",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.7.3"
  }
}
